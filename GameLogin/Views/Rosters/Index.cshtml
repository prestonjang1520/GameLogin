@model IEnumerable<GameLogin.Models.GameLogin.Roster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RosterName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RosterName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RosterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RosterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RosterId })
        </td>
    </tr>
}

</table>
